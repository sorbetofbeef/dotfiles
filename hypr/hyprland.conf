# This is an example Hyprland config file.
#
# Refer to the wiki for more information.

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=eDP-1, 3456x2160@60, 96x1080, 2
monitor=DP-2, 3840x2160@60, 0x0, 2

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

source = ~/.config/hypr/scripts/vars.conf
# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf


# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = caps:ctrl_modifier,altwin:swap_lalt_lwin
    kb_rules =
    repeat_rate=50
    repeat_delay=240
    sensitivity = 0.4 # -1.0 - 1.0, 0 means no modification.
    accel_profile = adaptive

    follow_mouse = true

    touchpad {
      disable_while_typing = true
	    natural_scroll = true
      clickfinger_behavior = true
      # middle_button_emulation=true
      tap-to-click = true
    }

}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 2
    gaps_out = 5
    border_size = 2
    col.active_border = rgba(88c0d0ff) rgba(81a1c1aa) 45deg
    col.inactive_border = rgba(2e3440ff)

    layout = master
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5
    blur = true
    blur_size = 15
    blur_passes = 3
    blur_new_optimizations = true

    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(cf9e2aaa)
    shadow_scale = 1.0
    shadow_offset = [0, 4]

    active_opacity = 0.98
    inactive_opacity = 0.90
}

   # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

animations {
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 8, myBezier, popin 80%
    animation = windowsOut, 1, 5, myBezier, popin 90%
    animation = border, 1, 10, default 
    animation = borderangle, 1, 20, myBezier, once
    animation = fade, 1, 2, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = true # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
# device:epic-mouse-v1 {
#     sensitivity = -0.5
# }

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

workspace=1, monitor:DP-2, default:true
workspace=2, monitor:eDP-1, default:true

binds {
  workspace_back_and_forth = true
}

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

windowrulev2 = float,center,class:(ide)

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, kitty
bind = $mainMod CTRL, E, exec, ide
bind = $mainMod, W, killactive,
bind = $mainMod CTRL, Q, exit,
bind = $mainMod, F, togglefloating,

bindr = $mainMod, SPACE, exec, pkill wofi || wofi --show drun
bindr = $mainMod, Q, exec, pkill wofi || wofi-power-menu
bindr = $mainMod, P, exec, pkill wofi || wofi-pass-menu

binde = ,XF86AudioLowerVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
binde = ,XF86AudioRaiseVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+
bind = ,XF86AudioMute,         exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind = ,XF86AudioMicMute,      exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
binde = ,XF86MonBrightnessUp,   exec, brightnessctl s 10%+
binde = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-
bind = ,XF86AudioPlay,         exec, playerctl play-pause
bind = ,XF86AudioPrev,         exec, playerctl previous
bind = ,XF86AudioNext,         exec, playerctl next
# bind = $mainMod, P, pseudo, # dwindle
# bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, H, workspace, e-1
bind = $mainMod, L, workspace, e+1
bind = $mainMod, K, layoutmsg, cycleprev
bind = $mainMod, J, layoutmsg, cyclenext
bind = $mainMod SHIFT, Return, layoutmsg, swapwithmaster auto
bind = $mainMod SHIFT, K, layoutmsg, swapprev
bind = $mainMod SHIFT, J, layoutmsg, swapnext


# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
# bind = $mainMod, 5, workspace, 5
# bind = $mainMod, 6, workspace, 6
# bind = $mainMod, 7, workspace, 7
# bind = $mainMod, 8, workspace, 8
# bind = $mainMod, 9, workspace, 9
# bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
# bind = $mainMod SHIFT, 5, movetoworkspace, 5
# bind = $mainMod SHIFT, 6, movetoworkspace, 6
# bind = $mainMod SHIFT, 7, movetoworkspace, 7
# bind = $mainMod SHIFT, 8, movetoworkspace, 8
# bind = $mainMod SHIFT, 9, movetoworkspace, 9
# bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Some default env vars.
env = XCURSOR_THEME, $cursor
env = XCURSOR_SIZE, $cursor_size
env = GTK_THEME, $theme
env = XDG_CURRENT_DESKTOP, Hyprland
env = XDG_SESSION_TYPE, wayland
env = XDG_SESSION_DESKTOP, Hyprland


exec-once = swww init &
exec = hyprctl setcursor $cursor $cursor_size
exec = gsettings set org.gnome.desktop.interface gtk-theme $theme
exec = gsettings set org.gnome.desktop.interface icon-theme $icon_theme
exec = gsettings set org.gnome.desktop.interface cursor-theme $cursor
exec = gsettings set org.gnome.desktop.interface cursor-size $cursor_size
exec = gsettings set org.gnome.desktop.wm.preferences theme $theme
exec = gsettings set org.gnome.desktop.wm.preferences button-layout "close,maximize,minimize:appmenu"
exec = swww img /home/me/media/pics/wp/mononoke_wp.jpg

exec-once = screen-timeout.sh 
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
exec-once = systemctl --user import-environment WAYLAND_DISPLAY DISPLAY SSH_AUTH_SOCK HYPRLAND_CMD HYPRLAND_INSTANCE_SIGNATURE XDG_CURRENT_DESKTOP XDG_SESSION_TYPE; dbus-update-activation-environment --systemd WAYLAND_DISPLAY DISPLAY SSH_AUTH_SOCK HYPRLAND_INSTANCE_SIGNATURE HYPRLAND_CMD XDG_CURRENT_DESKTOP XDG_SESSION_TYPE; systemctl --user start hyprland-session.target
